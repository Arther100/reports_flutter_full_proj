# Database Configuration (for reference - actual MS SQL connection via Node.js bridge)
DB_SERVER=208.91.198.174
DB_PORT=1433
DB_NAME=RuposPreProd
DB_USER=RuposPreProd
DB_PASSWORD=RuposPreProd$

# Dart Server Configuration (runs on port 3000, Node.js bridge on 5000)
PORT=3000
HOST=localhost

# Optional: Enable debug logging
DEBUG=true
